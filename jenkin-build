#!/bin/bash

# Access the values as environment variables
name="$NAME"
age="$AGE"
email="$EMAIL"

# You can use the values in your script
echo "Running jenkin-build script..."
echo "Name: $name"
echo "Age: $age"
echo "Email: $email"

# Add your build logic here
# Customize the build process using these values
