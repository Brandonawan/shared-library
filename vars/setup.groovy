// vars/setup.groovy
def call() {
    // This is the setup function that will be automatically executed
    echo "This is the setup function of the shared library."
}

// Define a default function if needed
def defaultFunction() {
    echo "This is the default function of the shared library."
}
